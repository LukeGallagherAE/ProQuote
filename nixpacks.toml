# Cache Puppeteer's Chrome download between Railway builds.
# First build still downloads ~170MB, but subsequent builds reuse the cache (~2min).
[phases.install]
cmds = ["npm ci"]
cacheDirectories = ["/root/.cache/puppeteer", "/root/.npm"]
